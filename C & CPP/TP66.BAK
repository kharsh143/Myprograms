#include<stdio.h>
#include<conio.h>
struct emp
{
char name[10];
int age;
};

void main()

{
FILE *p,*q;
struct emp e;
p=fopen("abc12.txt","w");
q=fopen("abc12.txt","r");
printf("enter the name & age");
scan("%s%d",e.name,&e.age);
fprintf(p,"%s%d",e.name,e.age);
fclose(p);
do
{
fscanf(q,"%s%d",e.name,e.age);
printf("%s%d".e.name,e.age);
}
while(!=EOF(q));
getch();
}