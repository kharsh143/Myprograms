#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
void main()
{
 int i,j,r,sum=0,temp,temp1;
 clrscr();
   for(i=0;i<=500;i++)
   {
	 temp=i;
	 temp1=i;
	 while(temp>0)
	 {
	   r=temp%10;
	   sum=sum+r*r*r;
	   temp=temp/10;
	 }
	  if(sum==temp1)
	  {
	    printf("%d\t",sum);
	  }
	  sum=0;

   }
 }